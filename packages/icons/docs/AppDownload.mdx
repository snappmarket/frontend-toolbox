---
name: App Download
route: /icons/AppDownload
menu: 4.Icons
---
import { Playground, Props } from 'docz';
import { Row, Col} from '../../ui/packages';
import * as Icons from '../packages/AppDownload';

# AppDownload icons

<Row>
  {Object.keys(Icons).map(icon => {
    const Icon = Icons[icon];
    return (
      <Col xs={3} className="text-center">
        <div className="bg-gray-ultra-light p-2">
          <Icon size={4} />
        </div>
        <code>{icon}</code>
      </Col>
    )
  })}
</Row>
