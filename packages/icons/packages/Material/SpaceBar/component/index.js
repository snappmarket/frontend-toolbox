/**
 * THIS IS AN AUTO GENERATED FILE, CHANGES WILL NOT APPLY
 */
import * as React from 'react';
import PropTypes from 'prop-types';

const SpaceBar = ({ className, size }) => (
  <svg
    data-testid="SpaceBar"
    viewBox="0 0 24 24"
    className={className}
    style={{
      width: size * 10,
      height: size * 10,
    }}
    focusable="false"
    fill="currentColor"
  >
    <path d="M18 9v4H6V9H4v6h16V9z"></path>, 'SpaceBar'
  </svg>
);

SpaceBar.propTypes = {
  className: PropTypes.string,
  size: PropTypes.number,
};

SpaceBar.defaultProps = {
  size: 1.5,
};

export default SpaceBar;
