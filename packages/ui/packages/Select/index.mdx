---
name: Select
route: /ui/select
menu: 3.Components
---
import { Playground, Props } from 'docz';
import { Row, Col } from '..';
import Input from './src/index';

export const statuses = ['gray', 'red', 'blue', 'orange', 'green', 'yellow'];

# Demo

<div className="mb-4">
  <div>
    <p>
      Input supports 6 different colors, label on top, and a message bellow it to show anything you want like errors and info
    </p>
    <Row>
    <Col xs={12} sm={6} md={4} className="mb-1">
        <Select
          id="subject"
          name="subject"
          label="please select options"
          options={statuses}
          autoComplete="off"
          message={{type: 'success', content:'this is a message'}}
        />
      </Col>
    </Row>
  </div>
</div>


# Basic Usage

<Playground>
        <Select
          id="subject"
          name="subject"
          label="please select options"
          options={['gray', 'red', 'blue']}
          autoComplete="off"
          message={{type: 'success', content:'this is a message'}}
        />
</Playground>

<br />
