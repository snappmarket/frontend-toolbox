---
name: Button
route: /ui/button
menu: 3.Components
---

import { Playground, Props } from 'docz';
import { Row, Col } from '..';
import Button from './src/index';

export const modifiers = ['primary', 'outline', 'link'];
export const sizes = ['xl', 'lg', 'md', 'sm', 'xs'];
export const colors = ['blue', 'red', 'green', 'yellow', 'orange', 'gray'];
export const shades = ['dark', 'normal', 'light', 'ultra-light', 'bright'];

# Demo
<div className="p-2 mb-4">
  <div>
    <h2>Colors and Shades</h2>
    <p>
      Buttons contains 6 different color collections and 5 shades
    </p>
    <Row className="mt-1">
      {colors.map(color => (
        <Col className="text-center" key={color} xs={2}>
          <h3>{color}</h3>
        </Col>
      ))}
    </Row>
    {shades.map(shade => (
      <Row key={shade} className="mt-1">
        {colors.map(color => (
          <Col key={color} xs={2}>
            <Button color={color} shade={shade} modifier='primary' size="sm" title={shade}/>
          </Col>
        ))}
      </Row>
    ))}
  </div>
  <div>
    <h2>Modifier and Sizes</h2>
    <p>
      Buttons contains 3 different modifiers and 5 sizes
    </p>
    <Row className="mt-1">
      {modifiers.map(modifier => (
        <Col className="text-center" key={modifier} xs={4}>
          <h3>{modifier}</h3>
        </Col>
      ))}
    </Row>
    {sizes.map(size => (
      <Row key={size} className="mt-1">
        {modifiers.map(modifier => (
          <Col key={modifier} xs={4} className="align-center justify-center">
            <Button modifier={modifier} size={size} title={size}/>
          </Col>
        ))}
      </Row>
    ))}
  </div>
</div>

# Basic Usage
<Playground>
  <Button
    modifier="primary"
    color="blue"
    title="Button"
    shade="normal"
    size="lg"
    onClick={() => alert('thanks for clicking me !')}
  />
</Playground>


# Props
<Props of={Button} />
