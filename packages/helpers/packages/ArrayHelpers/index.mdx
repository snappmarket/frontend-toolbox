---
name: ArrayHelpers
route: /helpers/ArrayHelpers
menu: Helpers
---




## Functions

<dl>
<dt><a href="#arrayItemAddProp">arrayItemAddProp(baseArray, props)</a> ⇒ array</dt>
<dd><p>adds similar properties to children of an array</p>
</dd>
<dt><a href="#flattenArray">flattenArray(array, property)</a> ⇒ array</dt>
<dd><p>makes a flat array from sub-arrays of an array</p>
</dd>
<dt><a href="#stringifyArray">stringifyArray(array, properties)</a> ⇒ string</dt>
<dd><p>Create string from array</p>
</dd>
<dt><a href="#deepFlatten">deepFlatten(array, property, flattenArrayTemp)</a> ⇒ Array</dt>
<dd><p>Deep flatten an array</p>
</dd>
</dl>

<a name="arrayItemAddProp"></a>

## arrayItemAddProp(baseArray, props) ⇒ array
adds similar properties to children of an array

**Kind**: global function  

| Param | Type | Description |
| --- | --- | --- |
| baseArray | array | the given array to add property to items |
| props | object | object that should be attached to each item |

<a name="flattenArray"></a>

## flattenArray(array, property) ⇒ array
makes a flat array from sub-arrays of an array

**Kind**: global function  

| Param | Type | Description |
| --- | --- | --- |
| array | array | given array to flatten sub-objects |
| property | string | name of the key of the of the sub-objects that want to be merged |

<a name="stringifyArray"></a>

## stringifyArray(array, properties) ⇒ string
Create string from array

**Kind**: global function  

| Param | Type | Description |
| --- | --- | --- |
| array | array | base array that wants to be stringify |
| properties | array | list of properties that want to be in stringified result |

<a name="deepFlatten"></a>

## deepFlatten(array, property, flattenArrayTemp) ⇒ Array
Deep flatten an array

**Kind**: global function  

| Param | Type | Description |
| --- | --- | --- |
| array | array | the multi dimensional array that want to be flatten |
| property | string | name of the property of array that want to navigate through |
| flattenArrayTemp | array | temp array to put the result of the flatten to |

