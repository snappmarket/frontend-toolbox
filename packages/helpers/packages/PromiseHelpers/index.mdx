---
name: PromiseHelpers
route: /helpers/PromiseHelpers
menu: Helpers
---
## PromiseHelpers
## Functions

<dl>
<dt><a href="#racePromise">racePromise(timeout)</a> ⇒ Promise.&lt;unknown&gt;</dt>
<dd><p>makes a race promise which returns rejection error on timeout</p>
</dd>
<dt><a href="#makeTimeout">makeTimeout(timeout)</a> ⇒ Promise.&lt;unknown&gt;</dt>
<dd><p>returns a rejected promise with TIMEOUT message</p>
</dd>
<dt><a href="#Empty promise to use as default things">Empty promise to use as default things()</a> ⇒ Promise.&lt;any&gt;</dt>
<dd></dd>
</dl>

<a name="racePromise"></a>

## racePromise(timeout) ⇒ Promise.&lt;unknown&gt;
makes a race promise which returns rejection error on timeout

**Kind**: global function  

| Param | Type | Description |
| --- | --- | --- |
| timeout | number | timout in ms to throw an error with resolve |

<a name="makeTimeout"></a>

## makeTimeout(timeout) ⇒ Promise.&lt;unknown&gt;
returns a rejected promise with TIMEOUT message

**Kind**: global function  

| Param | Type | Description |
| --- | --- | --- |
| timeout | number | timout in ms to throw an error with promise reject |

<a name="Empty promise to use as default things"></a>

## Empty promise to use as default things() ⇒ Promise.&lt;any&gt;
**Kind**: global function  
